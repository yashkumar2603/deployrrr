{
  "name": "deployrrr-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker request handler for Deployrrr.",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev --local --persist-to .wrangler/state",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260804.1",
    "typescript": "^5.3.3",
    "wrangler": "^4.27.0"
  }
}
